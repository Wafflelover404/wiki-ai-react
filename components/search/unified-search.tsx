/**
 * @deprecated
 * This component has been consolidated into app/app/admin/search/page.tsx
 * which is now the single source of truth for the search UI.
 * 
 * All search functionality is now accessed via /app/admin/search and
 * /app/search redirects to that page.
 */

export { UnifiedSearch } from './deprecated'
